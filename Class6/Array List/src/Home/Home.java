package Home;

public class Home {

	public static void main(String[] args) {
		/*
		 * int[] a = new int[100];
		 * 
		 * for (int i = 0; i < 100; i++) { a[i] = i + 1; }
		 * 
		 * int addAll = 0; for (int i = 0; i < 100; i++) { addAll += a[i]; }
		 * System.out.print(addAll);
		 */

		// Sort_int sortInt = new Sort_int();
		// sortInt.Sort_Int();
		//
		// Sort_String sortString = new Sort_String();
		// sortString.sort_string();
		//
		// Search search = new Search();
		// search.Search_String();

		array_list listArray = new array_list();
		listArray.create();
	}
}
